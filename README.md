# the-paint-project-contract

Compile 
`truffle compile`

Run tests
`truffle test`

Deploy onto the Rinkeby testnet
`truffle migrate --network rinkeby`

or, if you've already deployed to Rinkeby, reset and deploy:
`truffle migrate --reset --network rinkeby`